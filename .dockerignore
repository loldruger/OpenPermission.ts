# Git 관련 파일 무시
.git
.gitignore

# 의존성 폴더 무시 (컨테이너 안에서 새로 설치됨)
node_modules

# 컴파일 결과물 폴더 무시 (컨테이너 안에서 새로 생성됨)
dist

# 환경변수 파일 무시 (Cloud Run에서 직접 주입해야 함)
.env

# Dockerfile 자체는 이미지에 포함될 필요 없음
Dockerfile
